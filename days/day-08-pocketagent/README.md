# PocketAgent

Offline CLI chat built around the local Ollama API to run LLM inference on the edge.

## Tech Stack
- Python `urllib`
- Ollama

## Setup
```bash
source ../../.venv/bin/activate
cd days/day-08-pocketagent
python pocketagent.py
```
